# خطة العمل - تحديثات مشروع DXFix

## قائمة المهام

- [x] 1. **جاهزية Google AdSense** - تم التحقق: `ads.txt` موجود وصحيح، نشر المعرّف `ca-pub-8107638298388341` في `.env` والكود، صفحات الخصوصية/الشروط/الاسترداد موجودة، نصوص AdSense/Monetag تُحمَّل فقط بعد موافقة الكوكيز
- [x] 2. **عداد التحليلات في Vercel** - تم: تخطّي تحليلات Vercel لـ localhost/127.0.0.1/`admin=true`/`skip_analytics=1`/`debug=true` + إضافة `dxfix_is_owner=1` ككاشف لزيارات المالك
- [x] 3. **اللغات الفرنسية والصينية** - تم: إزالة `fr` و`zh` نهائياً من `i18n.ts` و`__root.tsx` (أصبحت العربية والإنجليزية فقط)
- [x] 4. **أدوات "قريباً" المدمجة** - تم: تنظيف خارطة الطريق والرسالة في `whatsapp-channel-info.ts` — حاسبة التكلفة ومحوّل الصور أصبحا منفذين، وأصبح G-Code Checker هو ما تبقى
- [x] 5. **كتابة ميزات الأداة على الواجهة الرئيسية** - تم مسبقاً (قسم "ماذا تريد أن تفعل؟" + المزايا الست)
- [x] 6. **نظام الإعلانات مع مشاهدة إجبارية** - تم مسبقاً: `AdGateModal` مع مؤقّت (7 ثوانٍ) + نموذج بريد قبل تفعيل التحميل
- [x] 7. **قائمة البريد الإلكتروني** - تم مسبقاً: `/api/waitlist` مع تخزين دائم (Vercel KV) + تصدير CSV + `viral-launch.ts`
- [x] 8. **شركات إعلانات بديلة** - تم مسبقاً: 7 شبكات (AdSense, Media.net, Propeller/Monetag, Carbon, MGID, Infolinks, BuySellAds) مع آلية تفعيل حسب مفاتيح `.env`
- [x] 9. **البحث عن مشاريع مفتوحة المصدر مشابهة + إضافة ميزات جديدة** - تم: إضافة أداة **مدقق ومحسّن أكواد G-Code** كاملة (`/tools/g-code-checker` + محرك `src/lib/gcode-engine.ts`): فحص الحدود، معدل التغذية، كتل متعددة، أكواد غير معروفة، غوص عميق، تقدير زمن الدورة، وتصدير تقرير TXT
- [x] 10. **عرض الإعلان الأعلى مردوداً** - تم مسبقاً: نظام Waterfall حسب eCPM (AdSense أولاً ~$5.00 ثم الشبكات الأخرى) في `ad-networks.ts` و`AdBanner.tsx`

## ملاحظات إضافية

- أداة G-Code الجديدة معرّفة في `src/routes/tools/g-code-checker.tsx` ومحركها النقي في `src/lib/gcode-engine.ts`
- أُضيف رابط الأداة في الواجهة الرئيسية (`src/routes/index.tsx`) و`floating-nav.tsx`
- تم التحقق: `npm run typecheck` و`npm run build` يمران بنجاح (exit 0)